# cofluhab

## Preparar para deploy Railway

```bash
git init
git add .
git commit -m "Preparar para deploy Railway"
git remote add origin https://github.com/<YOUR_USERNAME>/cofluhab.git
git push -u origin main
```
