# cofluhab

## Preparar para deploy Railway

```bash
git init
git add .
git commit -m "Preparar para deploy Railway"
git remote add origin https://github.com/SEU_USUARIO/cofluhab.git
git push -u origin main
```
